.claude/
.DS_Store

# Вихідні дані для збірки словників — важать сотні МБ.
# Завантажуються скриптом, у репозиторії не потрібні (див. data/README.md).
tools/*.jsonl
tools/*.tsv
tools/*.txt
tools/*.bz2
